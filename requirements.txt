# Mola Yönetim Sistemi — Python bağımlılıkları
#
# Yerel geliştirme:
#   uvicorn app.main:app --reload
#
# Netlify (serverless):
#   netlify/functions/api.py → Mangum handler

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
mangum>=0.19.0
sqlalchemy>=2.0.36
pydantic>=2.10.0
