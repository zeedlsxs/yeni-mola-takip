# Mola Yönetim Sistemi — Python bağımlılıkları
#
# Yerel geliştirme:
#   uvicorn app.main:app --reload
#
# Vercel (serverless):
#   api/index.py → FastAPI handler

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.36
pydantic>=2.10.0
